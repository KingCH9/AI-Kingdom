# Paste Railway Postgres DATABASE_URL for local inspection only if using a PUBLIC proxy URL.
# Internal host postgres.railway.internal is NOT reachable from your PC — use `railway run` instead.
# DATABASE_URL=postgresql://user:password@host:port/railway
