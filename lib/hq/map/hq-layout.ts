/** Canvas dimensions for the HQ map scene. */
export const HQ_MAP_WIDTH = 1024;
export const HQ_MAP_HEIGHT = 720;

/** Padding inside each room before agents are placed. */
export const ROOM_INNER_PADDING = 16;

/** Size of each agent marker on the map. */
export const AGENT_MARKER_SIZE = 44;

/** Vertical gap between agent rows inside a room. */
export const AGENT_ROW_GAP = 8;

/** Horizontal gap between agent columns inside a room. */
export const AGENT_COL_GAP = 8;
