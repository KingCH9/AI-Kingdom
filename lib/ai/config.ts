/** Default model for general-purpose Claude calls (askClaude). */
export const DEFAULT_CLAUDE_MODEL = "claude-sonnet-4-5-20250929";

/** Model used for full opportunity generation workflows. */
export const OPPORTUNITY_GENERATION_MODEL = "claude-sonnet-4-6";

export const DEFAULT_MAX_TOKENS = 500;

export const OPPORTUNITY_GENERATION_MAX_TOKENS = 4000;

/** Max tokens for product page generation (structured JSON sales copy). */
export const PRODUCT_PAGE_MAX_TOKENS = 4096;
